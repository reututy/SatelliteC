<UserControl x:Class="Presentation.ComponentsTabs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation">
    <!--:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"-->
    <UserControl.Resources>
        <Style TargetType="WrapPanel">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="Width" Value="Auto"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <TabControl>
            <TabItem Header="EPS" Visibility="Visible">
                <WrapPanel x:Name="epspanel" HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto" Width="Auto"/>
            </TabItem>
            <TabItem Header="TRX" Visibility="Visible">
                <WrapPanel x:Name="trxpanel" HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto" Width="Auto"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
